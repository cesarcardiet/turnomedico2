import 'package:flutter/material.dart';

/// Paleta cálida y azul para Medica - Turno Médico.
class AppColors {
  AppColors._();

  // Azul principal (cálido y confiable)
  static const Color primary = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFF42A5F5);
  static const Color primaryDark = Color(0xFF0D47A1);

  // Acentos
  static const Color secondary = Color(0xFF4CAF50);
  static const Color accent = Color(0xFF0288D1);

  // Fondos (cálidos y suaves)
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Grises (suaves)
  static const Color light = Color(0xFFF1F5F9);
  static const Color lightBlue = Color(0xFFE3F2FD);
  static const Color gray = Color(0xFF64748B);
  static const Color grayLight = Color(0xFF94A3B8);

  // Texto
  static const Color black = Color(0xFF1E293B);
  static const Color white = Color(0xFFFFFFFF);

  // Estados
  static const Color red = Color(0xFFDC2626);
  static const Color orange = Color(0xFFF59E0B);
  static const Color green = Color(0xFF10B981);

  // Inputs
  static const Color inputFill = Color(0xFFF1F5F9);
  static const Color inputBorder = Color(0xFFE2E8F0);

  // Banner y categorías (azul suave)
  static const Color teal = Color(0xFF0EA5E9);
}
